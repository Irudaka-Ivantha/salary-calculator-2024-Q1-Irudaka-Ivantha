import React from 'react';

const Button = () => {
  return (
    <div className="absolute top-4 right-4 flex justify-center items-center">
      <button className="text-blue-700 text-sm font-medium">Reset</button>
    </div>
  );
};

export default Button;
